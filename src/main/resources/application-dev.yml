spring:
  datasource:
    url: jdbc:mysql://localhost:3306/health_care?useSSL=false&serverTimezone=UTC
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true